import { MainContainer } from "@/containers";
import { NextPage } from "next";

const CustomNotFoundPage: NextPage = () => {
  return <MainContainer>Not found page..</MainContainer>;
};

export default CustomNotFoundPage;
