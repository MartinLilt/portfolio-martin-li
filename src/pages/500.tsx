import { MainContainer } from "@/containers";
import { NextPage } from "next";

const CustomServerErrorPage: NextPage = () => {
  return <MainContainer>Server error page..</MainContainer>;
};

export default CustomServerErrorPage;
