export enum ModalEnums {
  mobileMenu = 'modal/mobileMenu',
  cookiesModal = 'modal/cookies',
}

export enum ModalCookieEnums {
  cookieConsentAll = 'cookie-consent-all',
  cookieRejectionAll = 'cookie-rejection-all',
  cookieConsentCustom = 'cookie-consent-custom',
}